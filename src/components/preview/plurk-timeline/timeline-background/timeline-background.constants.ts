/**
 * 用途：定義河道背景 feature 在 store / 匯出 CSS 使用的正式 selector。
 *
 * 目前 UI 稱為「河道背景」，但為了保留既有草稿與匯出相容性，實際 selector 仍是 body。
 *
 * Editable props:
 * - backgroundImage
 * - backgroundSize
 * - backgroundRepeat
 * - backgroundPosition
 * - backgroundAttachment
 */
export const TIMELINE_BACKGROUND_SELECTOR = "body";
