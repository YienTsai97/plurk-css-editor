/**
 * 用途：定義時間軸裝飾 feature 在 store / 預覽 / 匯出 CSS 使用的正式 selector。
 *
 * 對齊 Plurk 實站與預覽 DOM（#layout_content._lc_ > .timeline-bg）。
 *
 * Editable props:
 * - backgroundImage
 * - backgroundSize
 * - backgroundRepeat
 * - backgroundPosition
 */
export const TIMELINE_DECORATION_SELECTOR = "._lc_ .timeline-bg";
