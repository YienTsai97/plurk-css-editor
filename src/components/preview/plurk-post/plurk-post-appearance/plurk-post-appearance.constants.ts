/**
 * 用途：定義貼文主體在 store / 匯出 CSS 使用的正式 selector。
 *
 * Editable props:
 * - backgroundColor
 * - backgroundImage
 * - border
 * - borderRadius
 */
export const PLURK_POST_SELECTOR = ".plurk_cnt";

/**
 * 用途：定義貼文暱稱在 store / 匯出 CSS 使用的正式 selector。
 *
 * Editable props:
 * - color
 */
export const PLURK_POST_NAME_SELECTOR = ".name";
